{
  "name": "imbriani-stefano-noleggio",
  "version": "8.2.0",
  "private": true,
  "scripts": {
    "apps:login": "npx clasp login --no-localhost",
    "apps:pull": "npx clasp pull",
    "apps:push": "npx clasp push -f",
    "apps:deploy": "npx clasp deploy -d 'Deploy v8.2'",
    "apps:open": "npx clasp open",
    "apps:publish": "npm run apps:push && npm run apps:deploy && npm run apps:open"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
